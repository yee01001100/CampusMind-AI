# Mock mode is the safe default and never needs a credential.
VITE_USE_MOCKS=true
VITE_API_BASE_URL=http://127.0.0.1:8000

# Never put model provider keys in VITE_* variables: Vite exposes them to browsers.
